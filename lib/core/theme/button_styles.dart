import 'package:flutter/material.dart';

class BtnStyle {
  static final titleBar = IconButton.styleFrom(
    padding: EdgeInsets.symmetric(horizontal: 8, vertical: 4),
    iconSize: 20,
  );

  static final mainMenu = IconButton.styleFrom(
    padding: EdgeInsets.all(8),
    iconSize: 20,
  );
}
