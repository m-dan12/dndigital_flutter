import 'package:flutter/material.dart';

class ButtonStyles {
  static final titleBarIcon = IconButton.styleFrom(
    padding: EdgeInsets.symmetric(horizontal: 8, vertical: 4),
    iconSize: 20,
  );

  static final mainMenuIcon = IconButton.styleFrom(
    padding: EdgeInsets.zero,
    iconSize: 24,
  );

  static final navPanelIcon = IconButton.styleFrom(
    padding: EdgeInsets.zero,
    iconSize: 20,
  );
  static final smallButton = IconButton.styleFrom(
    padding: EdgeInsets.symmetric(horizontal: 8, vertical: 4),
  );
}
